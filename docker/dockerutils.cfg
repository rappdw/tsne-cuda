[test]
tag=latest

[build]
volumes=--mount type=bind,source=/data/workspaces/{user}/code/{project},target=/workdir -v /data:/data
volumes_docked=--mount type=bind,source=/data/workspaces/{user}/code/{project},target=/workdir -v /data:/data
