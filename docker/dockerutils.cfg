[test]
tag=latest
volumes=--mount type=bind,source=/data/workspaces/{user}/code/{project},target=/build -v /data:/data

[build]
volumes=--mount type=bind,source=/data/workspaces/{user}/code/{project},target=/workdir -v /data:/data
